@model IEnumerable<LMS_MVC.Models.Student>

@{
    ViewData["Title"] = "Index";
}

<h5>Student Details</h5>

<p>
    <button><a asp-action="Create">New Student Entry</a></button>
   @* <a asp-action="ViewAll">View All Student</a>*@
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StudentRollNo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudentName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Department)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Semester)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudentContact)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudentEmail)
            </th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StudentRollNo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudentName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Department)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Semester)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudentContact)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudentEmail)
            </td>
            <td>
               <button><a asp-action="Edit" asp-route-id="@item.StudentID">Edit</a></button> 
               <button> <a asp-action="Details" asp-route-id="@item.StudentID">Details</a> </button>
               <button> <a asp-action="Delete" asp-route-id="@item.StudentID">Delete</a></button>
            </td>
        </tr>
}
    </tbody>
</table>
